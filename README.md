# localization-app-front

instalacja po sklonowaniu:

`npm install`  

w pliku `src/consts/config.js` znjaduje się konfiguracja połączenia z backendem

uruchomienie aplikacji

`npm run start`  

domyślnie działa na porcie 3000

strona logowania:
`http://localhost:3000/login`